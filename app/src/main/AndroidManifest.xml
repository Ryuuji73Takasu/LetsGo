<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mrhsourav54.letsgo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Homepage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Currency_conv" />
        <activity android:name=".LoginCompany" />
        <activity android:name=".GiveCompanyData" />
        <activity android:name=".viewAgencyDetail" />
        <activity android:name=".CompareHotels" />
        <activity android:name=".radToWestin" />
        <activity android:name=".lakeshoreToWestin" />
        <activity android:name=".rad_to_lakeshore" />
        <activity android:name=".rad_to_platinumSuites" />
        <activity android:name=".radission_to_sheraton"></activity>
    </application>

</manifest>